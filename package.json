{
  "name": "graphql-server",
  "version": "1.0.0",
  "description": "Another GraphQL server with Apollo and MongoDB",
  "dependencies": {
    "graphql": "^14.0.2",
    "apollo-server": "^2.1.0",
    "mongoose": "^5.3.2"
  },
  "devDependencies": {
    "nodemon": "^1.18.4",
    "dotenv": "^6.1.0"
  },
  "scripts": {
    "start": "nodemon --watch src ./index.js"
  }
}
